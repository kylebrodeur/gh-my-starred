#!/usr/bin/env bash
# Thin wrapper: gh-my-starred CLI now lives in Node.js
# This shim delegates to src/cli.js for backward compat with gh extensions
set -e
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec node "${SCRIPT_DIR}/src/cli.js" "$@"
